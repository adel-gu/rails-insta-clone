%h1= @pic.title
%p= @pic.description

= link_to "Back", root_path
= link_to "Edit", edit_pic_path(@pic)
= link_to "Delete", pic_path(@pic), data: { turbo_method: :delete, turbo_confirm: "Are you sure!" }